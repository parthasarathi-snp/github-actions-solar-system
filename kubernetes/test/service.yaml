apiVersion: v1
kind: Service
metadata:
  name: solarsystem-service
  namespace: _{_NAMESPACE_}_
spec:
  selector:
    app: solar-system 
  ports:
    - port: 80
      targetPort: 80
  type: LoadBalancer
